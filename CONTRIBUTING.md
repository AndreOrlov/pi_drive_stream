# Contributing to Pi Drive Stream

Спасибо за интерес к проекту! Вот как можно внести свой вклад.

## Процесс разработки

### 1. Форк и клонирование

```bash
git clone https://github.com/your-username/pi_drive_stream.git
cd pi_drive_stream
```

### 2. Создание ветки

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

### 3. Настройка окружения

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install pytest ruff mypy pre-commit
```

### 4. Установка pre-commit hooks (рекомендуется)

```bash
pre-commit install
```

Теперь перед каждым коммитом будут автоматически запускаться проверки:
- Форматирование кода (ruff)
- Проверка trailing whitespace
- Проверка YAML/TOML файлов

### 5. Разработка

- Пишите чистый, документированный код
- Следуйте PEP 8 (ruff проверит автоматически)
- Добавляйте type hints
- Пишите docstrings для функций и классов

### 6. Тестирование

```bash
# Запустить все тесты
pytest tests/ -v

# Запустить с покрытием
pytest tests/ --cov=app --cov-report=term-missing

# Запустить линтер
ruff check app/ tests/

# Запустить type checker
mypy app/ --ignore-missing-imports
```

### 7. Коммит

```bash
git add .
git commit -m "feat: добавлена новая функция X"
```

**Формат commit message:**
- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `test:` — добавление/изменение тестов
- `refactor:` — рефакторинг кода
- `style:` — форматирование, без изменения логики
- `chore:` — обновление зависимостей, CI/CD

### 8. Push и Pull Request

```bash
git push origin feature/your-feature-name
```

Затем создайте Pull Request на GitHub в ветку `dev`.

## Требования к Pull Request

### ✅ Обязательно:
- [ ] Все тесты проходят (`pytest tests/`)
- [ ] Линтер не выдаёт ошибок (`ruff check`)
- [ ] Код покрыт тестами (для новой функциональности)
- [ ] Заполнен шаблон PR
- [ ] Нет конфликтов с целевой веткой

### 📝 Желательно:
- [ ] Добавлены docstrings
- [ ] Обновлена документация (README.md, PLAN_OSD.md)
- [ ] Протестировано на Raspberry Pi (для hardware-related изменений)

## CI/CD Pipeline

После создания PR автоматически запустятся проверки:
1. **Linter** (ruff) — проверка стиля кода
2. **Type checker** (mypy) — проверка типов
3. **Tests** (pytest) — запуск всех тестов
4. **Build check** — проверка синтаксиса Python

PR можно смерджить только если все проверки прошли успешно (✅).

## Branch Protection

- **`master`** — production-ready код, требуется review
- **`dev`** — разработка, требуется прохождение CI
- Прямой push в `master` и `dev` разрешён только maintainer'у

## Структура проекта

```
pi_drive_stream/
├── app/
│   ├── overlay/        # OSD система
│   ├── nodes/          # Control nodes
│   ├── hw/             # Hardware drivers
│   └── web/            # Web server
├── tests/              # Тесты
├── frontend/           # Web UI
└── .github/            # CI/CD конфигурация
```

## Стиль кода

- Следуем PEP 8
- Используем type hints везде, где возможно
- Docstrings в формате Google Style
- Максимальная длина строки: 100 символов (настроено в ruff)

## Вопросы?

Создайте issue на GitHub или свяжитесь с maintainer'ом.

Спасибо за вклад! 🚀
